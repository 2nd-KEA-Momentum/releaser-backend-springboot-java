server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://releaser.ciycwesqyh7a.ap-northeast-2.rds.amazonaws.com/releaser
    username: admin
    password: momentum!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
        use_sql_comments: true

logging.level:
  org.hibernate.SQL: debug